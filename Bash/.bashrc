####################################################################################################################################
####################################################################################################################################
# Redirect to ".bash_profile" for consistency between shells
if [[ -f "$HOME/.bash_profile" ]] ; then
    source "$HOME/.bash_profile"
fi
